target:
  ignorePath: '../../static/'
  src: 'templates/core/base.html'
  fileTypes:
    html:
      replace:
        js: '<script src="{{ STATIC_URL }}{{filePath}}"></script>'
  overrides:
    lodash:
      main: 'dist/lodash.js'
